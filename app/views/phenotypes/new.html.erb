<h1><%= @title = "Add Phenotype" %></h1>

<% form_for(@phenotype) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'fields', :locals => {:f => f} %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>
