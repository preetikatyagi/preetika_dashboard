<h1><%= @title = 'Add Data Source' %></h1>

<% form_for(@source) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => 'fields', :locals => {:f => f} %>
	
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>
