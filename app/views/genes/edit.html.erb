<h1><%= @title = "Editing #{@gene.hugo_id}" %></h1>

<% form_for(@gene) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'fields', :locals => {:f => f} %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>
