<h1><%= @title = "Edit Drug" %></h1>
<%= link_to image_tag('icons/eye.png'), @drug, :class => 'quick_link' %>
<%= link_to image_tag('icons/application_view_list.png'), drugs_path, :class => 'quick_link' %>


<% form_for(@drug) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'fields', :locals => {:f => f} %>

    <%= f.submit 'Update' %>
  </p>
<% end %>

